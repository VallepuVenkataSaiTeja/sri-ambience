import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'Sri Ambience - The City Pad | Premium PG Accommodation',
  description: 'Experience comfortable and affordable PG accommodation at Sri Ambience the city pad. Modern amenities, prime location, and a home away from home.',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  )
}

